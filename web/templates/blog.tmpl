{{ template "before" . }}
{{ template "header" . }}

<main>
  <h1>{{.PageTitle}}</h1>
  <p>{{.Body}}</p>
  <section>
    {{ template "partials/latest" . }}
  </section>
</main>

{{ template "footer" . }}
{{ template "after" . }}
